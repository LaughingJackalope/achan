# Development profile configuration
# This profile is automatically activated when running `./gradlew quarkusDev`
quarkus:
  # Disable OpenTelemetry in local development to avoid warning logs
  # Enable in production by setting the 'prod' profile or removing this override
  otel:
    sdk:
      disabled: true
